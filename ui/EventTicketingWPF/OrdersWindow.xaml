<Window x:Class="EventTicketingWPF.OrdersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="My Orders &amp; Tickets" Height="500" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="📋 Order History" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>
        <DataGrid Grid.Row="1" Name="OrdersDataGrid" AutoGenerateColumns="True" IsReadOnly="True" Margin="0,0,0,10"/>
    </Grid>
</Window>